# Development dependencies for 3dsconv
#
# RECOMMENDED INSTALLATION:
#   pip install -e ".[dev]"
#
# This file is maintained for compatibility but pyproject.toml is the
# authoritative source. The above command installs all dependencies
# (production + dev) from pyproject.toml.
#
# If you must use this file directly:
#   pip install -r requirements-dev.txt

# Production dependencies
-r requirements.txt

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Code quality
black>=23.0.0
ruff>=0.1.0
mypy>=1.0.0
